---
phase: 01-dependency-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/bot/go.mod
  - backend/bot/go.sum
autonomous: true

must_haves:
  truths:
    - "Go backend compiles without errors on Go 1.25.6"
    - "Go backend tests pass on Go 1.25.6"
    - "go.mod reflects Go 1.25.6 version"
  artifacts:
    - path: "backend/bot/go.mod"
      provides: "Go module configuration"
      contains: "go 1.25.6"
    - path: "backend/bot/go.sum"
      provides: "Dependency checksums"
  key_links:
    - from: "backend/bot/go.mod"
      to: "go toolchain"
      via: "go directive"
      pattern: "go 1\\.25\\.6"
---

<objective>
Upgrade Go backend toolchain from 1.25.3 to 1.25.6

Purpose: Apply Go 1.25.6 security patch (released 2026-01-15) to the backend service. This is the simplest of the four dependency upgrades and establishes the pattern for sequential verification.

Output: Updated go.mod with Go 1.25.6, verified build and tests passing
</objective>

<execution_context>
@/home/tdolfen/.claude/get-shit-done/workflows/execute-plan.md
@/home/tdolfen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-dependency-modernization/01-RESEARCH.md

# Source files
@backend/bot/go.mod
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Go toolchain to 1.25.6</name>
  <files>backend/bot/go.mod, backend/bot/go.sum</files>
  <action>
Run the following commands from the backend/bot directory:

```bash
cd backend/bot
go get go@1.25.6
go mod tidy
```

Expected behavior per RESEARCH.md:
- Go 1.25 will update the `go` directive from `go 1.25` to `go 1.25.6`
- The `toolchain go1.25.3` line will be REMOVED (not updated) because Go 1.25 omits the toolchain line when it equals the go directive
- This is the correct Go 1.25 behavior; do NOT manually add a toolchain line back

After running, verify go.mod contains:
```
go 1.25.6
```
And does NOT contain a `toolchain` line (this is correct).
  </action>
  <verify>
```bash
cd backend/bot
grep "go 1.25.6" go.mod  # Should match
grep "toolchain" go.mod  # Should NOT match (exit 1 is expected)
```
  </verify>
  <done>go.mod contains "go 1.25.6" and no toolchain line</done>
</task>

<task type="auto">
  <name>Task 2: Verify Go backend builds and tests pass</name>
  <files>backend/bot/</files>
  <action>
Build the entire backend module and run all tests:

```bash
cd backend/bot
go build ./...
go test ./...
```

Both commands must exit 0. If tests require a database or Slack connection, they should be skipped or mocked appropriately -- check the test output for skip messages vs failures.
  </action>
  <verify>
```bash
cd backend/bot
go build ./... && echo "BUILD OK"
go test ./... && echo "TESTS OK"
```
  </verify>
  <done>Go build succeeds (exit 0), Go tests pass or skip appropriately (exit 0)</done>
</task>

</tasks>

<verification>
After completing both tasks:

1. go.mod shows `go 1.25.6`
2. No `toolchain` line present in go.mod (correct Go 1.25 behavior)
3. `go build ./...` exits 0
4. `go test ./...` exits 0

DEP-02 (Go backend updated to 1.25.6) will be satisfied.
</verification>

<success_criteria>
- Go 1.25.6 is the active toolchain version
- Backend code compiles without errors
- All existing tests pass
- No regressions introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-modernization/01-01-SUMMARY.md`
</output>
